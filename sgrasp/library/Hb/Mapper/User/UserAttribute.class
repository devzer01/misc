<?php 
/**
 * Mapper Object for User Attribute
 * 
 * @copyright 2007 Global Market Insite Inc
 * @author harsha
 * @version 1.1
 * @package Mapper
 * @subpackage User
 *
 */ 
class Hb_Mapper_User_UserAttribute extends Hb_Mapper_Mapper 
{
	/**
	 * Primary key
	 *
	 * @var string 
	 */
	protected $primary_key = 'user_attr_id';
	
	/**
	 * table
	 *
	 * @var string 
	 */
	protected $table = 'user_attr';
	
	/**
	 * Builds and returns the User Attribute domain object for this mapper
	 *
	 * @param $row $object Result row from the Zend_Db framework for the user_attr table
	 * @return object An instance of the User Attribute domain object 
	 */	
	protected function doBuildObject($row)
	{
		return new Hb_App_User_UserAttribute($row->user_attr_id,
									$row->login,
									$row->user_attr,
									$row->user_value
									);
	} 
	
	/**
	 * Builds and returns a Zend_Db Select object
	 *
	 * @return $select object Result Select object from the Zend_Db framework 
	 */
	protected function GetBaseSelect()
	{
		$db 		= Hb_Util_Db_Connection::GetInstance();
		$select 	= $db->select();
	 	$select->from(array('ua' => 'user_attr'), array('user_attr_id',
	 																	'user_value',
	 																	'user_attr',
	 																	'login')
	 					 );
	 	return $select;
	}
	
	/**
	 * Finds a User Attribute by it's Id
	 *
	 * @param $id int The record identifier that uniquely identifies the row.
	 * @return obj Result row from the Zend_Db framework for the user_attr table
	 */	
	protected function doFindById($id)
	{
	 	$select = $this->GetBaseSelect();
		$select->where('ua.'. $this->primary_key .'= ?', $id);
		 	
		return $this->getRow($select);
	}
	
	/**
	 * Saves a dirty domain object to the persistence layer
	 *
	 * @param Hb_App_Object $obj object Instance of the User Attribute domain object
	 */
	protected function doSave(Hb_App_Object $obj)
	{
//		print_r($obj);
//		print $obj->GetUserId();
//		die();
		
	 	require_once('class/Hb/Util/Db/Query.class');
		
		$db = Hb_Util_Db_Query::getInstance();

		if (is_null($obj->GetId())) 
		{
			$query = "INSERT INTO user_attr 
							(login, 
							 user_attr, 
							 user_value ) "
               			. " VALUES ('" . $obj->GetUserId() . "', 
               			 			  '" . $db->escapeString($obj->GetAttributeName()) . "', 
               			 			  '" . $db->escapeString($obj->GetAttributeValue()) . "')";
                    			
			
			$db->q($query);
			
			$obj->SetUserAttributeId($db->getLastInsertId());			
			
		} 
		else
		{
			$query = "UPDATE user_attr "
						. "SET login = '" . $obj->GetUserId() . "', "
							. " user_attr = '" 	. $db->escapeString($obj->GetAttributeName()) . "', "
							. " user_value = '" . $db->escapeString($obj->GetAttributeValue()) . "' "
							. "WHERE user_attr_id = " . $obj->GetId();

			$db->q($query);
		}
	}
}

?>